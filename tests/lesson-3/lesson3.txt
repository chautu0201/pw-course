1.
git commit --amend: sử dụng dòng lệnh, thay đổi message commit gần nhất
git commit --amend -m”<message>”:  sửa đổi message commit gần nhất nhưng thay đổi trực tiếp không cần sử dụng các dòng lệnh tiếp theo
git restore --staged <file>: chuyển file từ vùng staging area sang vùng working directory
git reset HEAD~1: Chuyển file từ vùng responsitory về working directory (uncommit)

2.
a.Tạo 3 file: file1, file2, file3
    Vùng working: không có
    Vùng staging: không có
    Vùng responsitory: không có

b.Chạy lên git init
    Vùng working: file1, file2, file3
    Vùng staging: 
    Vùng responsitory: 

c.Chạy lệnh git commit -m”init project”
    Vùng working: file1, file2, file3
    Vùng staging: 
    Vùng responsitory: Tạo 1 commit với message "init project”

d.Chạy lệnh: git add file1
    Vùng working: file2, file3
    Vùng staging: file1 -> add file 1 từ vùng working -> staging
    Vùng responsitory: 

e.Chạy lệnh: git commit -m”add file”
     =Vùng working: file2, file3
    Vùng staging:
    Vùng responsitory: file1 -> chuyển file 1 từ vùng staging -> responsitory và tạo 1 commit với message "init project"

f. Chạy lệnh: git add .
    Vùng working: 
    Vùng staging: file2, file3 -> chuyển tất cả các file trong vùng working -> staging để chuẩn bị commit
    Vùng responsitory: file1

g. Chạy lệnh: git restore --staged file2
    Vùng working: file2 -> chuyển file 2 từ staging -> working
    Vùng staging: file3 
    Vùng responsitory: file1

h. Chạy lệnh: git commit -m”add all files”
    Vùng working: file2
    Vùng staging: file3
    Vùng responsitory: file1,file3 -> chuyển tất cả các file từ vùng staging -> responsitory với commit message "add all file"


i. Chạy lệnh: git reset HEAD~1
    Vùng working: file2, file3 -> uncommit. chuyển file đã add từ 1 commit trước đó từ vùng responsitory -> working
    Vùng staging:
    Vùng responsitory: file1

3. 
a. Chạy lệnh git init: 
    Vùng working: 
    Vùng staging:
    Vùng responsitory:

b. Tạo file
    Vùng working: file1, file2, oops/file3.txt
    Vùng staging:
    Vùng responsitory:

c. 
    Vùng working:  .gitignore
    Vùng staging:
    Vùng responsitory:
d.
    Vùng working:  
    Vùng staging: file1, file2, oops/file3.txt, .gitignore
    Vùng responsitory: 

e.
    Vùng working:  
    Vùng staging: 
    Vùng responsitory: file3

   